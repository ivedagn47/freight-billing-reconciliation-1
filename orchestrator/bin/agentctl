#!/usr/bin/env bash
set -euo pipefail
ORCH_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PY="$ORCH_ROOT/.venv/bin/python"
[ -x "$PY" ] || PY="$(command -v python3)"
PYTHONPATH="$ORCH_ROOT/lib" exec "$PY" -m agentctl "$@"
