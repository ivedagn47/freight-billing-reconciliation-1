You are the planning worker in an orchestrator drill.

Goal: {goal}

Write a JSON file at exactly this path: {plan}

The JSON must be:

{
  "_session_id": "<your session id>",
  "goal": "<the goal above, exactly>",
  "steps": ["<3 to 5 short step names: lowercase words joined by hyphens, all different>"]
}
